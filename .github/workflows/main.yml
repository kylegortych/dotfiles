name: main
on:
  push:
    branches:
      - main
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test configuration.nix
      - run: |
        nix-build '<nixpkgs/nixos-unstable>' -A config.system.build.toplevel -I nixos-config=./test.nix
